# Contact Launcher

A static mobile page that copies a prepared message and opens an allowed contact destination.

The URL fragment contains the destination and message. Browsers do not send fragments to GitHub Pages or other servers. The page has no analytics, network requests, or external assets.

Mobile Safari requires a user gesture before clipboard writes. When automatic copying is blocked, the page shows one button that copies the message and opens the contact.
